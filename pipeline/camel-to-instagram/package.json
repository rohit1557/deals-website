{
  "name": "camel-to-instagram",
  "version": "1.0.0",
  "private": true,
  "description": "Daily DealDrop Instagram post generator — fetches CCC top drops, generates 1080x1080 images",
  "scripts": {
    "generate": "ts-node index.ts",
    "build": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "pg": "^8.12.0",
    "puppeteer": "^22.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/pg": "^8.11.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
